spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/techsupport_staging}
    username: ${DATABASE_USER:techsupport_user}
    password: ${DATABASE_PASSWORD}
  
  jpa:
    show-sql: false

logging:
  level:
    com.techsupport: INFO
    org.springframework.modulith: INFO
