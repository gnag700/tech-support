plugins {
    id 'java'
}
repositories {
    maven { url 'https://repo.spring.io/milestone' }
}
configurations {
    check
}
dependencies {
    check 'org.springframework.modulith:spring-modulith-bom:2.0.0-RC1'
}
